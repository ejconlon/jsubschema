{

  "type" : "object",

  "id" : "http://exathunk.net/schemas/schema",

  "declarations" : {
    "stringArray" : {
      "type" : "array",
      "items" : {
        "type": "string"
      }
    }
  },

  "required": ["type", "$ref"],

  /*"forbidsMap" : {
    "type" : ["$ref"],
    "properties" : ["items"],
    "items" : ["properties"]
  },

  "allowsMap" : {
    "$ref" : []
  },

  "dependenciesMap" : {

  }, */

  "properties" : {

    "type" : {
      "type" : "string",
      "forbids" : ["$ref"]
    },

    "description" : {
      "type" : "string"
    },

    "format" : {
      "type" : "string"
    },

    "properties" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      }
    },

    "declarations" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      }
    },

    "id" : {
      "type" : "string",
      "forbids" : ["$ref"]
    },

    "$ref" : {
      "type" : "string",
      "forbids" : ["id", "description", "format", "properties", "declarations", "items", "type"]
    },

    "items" : {
      "$ref" : "#",
      "forbids" : ["properties"]
    },

    "requires" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    },

    "forbids" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    },

    "required" : {
      "type" : "array",
      "items" : {
        "type" : "string"
      }
    }

    /*, "dependenciesMap" : {
      "type" : "object",
      "items" : {
          "$ref" : "#/declarations/stringArray"
      }
    },

    "allowsMap" : {
      "type" : "object",
      "items" : {
        "$ref" : "#/declarations/stringArray"
      }
    },*/

    /*, "forbidsMap" : {
      "type" : "object",
      "items" : {
          "$ref" : "#/declarations/stringArray"
      }
    }*/
  }

}