{

  "type" : "object",

  "id" : "http://exathunk.net/schemas/schema",

  "declarations" : {
    "stringArray" : {
      "type" : "array",
      "items" : {
        "type": "string"
      }
    }
  },

  "required": ["type", "$ref"],

  "forbids" : {
    "type" : ["$ref"],
    "id" : ["$ref"],
    "properties" : ["items"],
    "items" : ["properties"],
    "$ref" : ["id", "description", "format", "properties", "declarations", "items",
              "type", "required", "allows", "forbidsMap", "dependencies"]
  },

  "properties" : {

    "type" : {
      "type" : "string"
    },

    "description" : {
      "type" : "string"
    },

    "format" : {
      "type" : "string"
    },

    "properties" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      }
    },

    "declarations" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      }
    },

    "id" : {
      "type" : "string"
    },

    "$ref" : {
      "type" : "string"
    },

    "items" : {
      "$ref" : "#"
    },

    "required" : {
      "type" : "array",
      "items" : {
        "type" : "string"
      }
    },

    "dependencies" : {
      "type" : "object",
      "items" : {
          "$ref" : "#/declarations/stringArray"
      }
    },

    "forbids" : {
      "type" : "object",
      "items" : {
          "$ref" : "#/declarations/stringArray"
      }
    }

  }

}