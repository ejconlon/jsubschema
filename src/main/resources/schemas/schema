{

  "type" : "object",

  "id" : "http://exathunk.net/schemas/schema",

  "properties" : {

    "type" : {
      "type" : "string",
      "required" : true
    },

    "description" : {
      "type" : "string"
    },

    "format" : {
      "type" : "string"
    },

    "properties" : {
      "type" : "object",
      "items" : {
        "type" : "object",
        "$ref" : "#"
      },
      "forbids" : ["items"]
    },

    "declarations" : {
      "type" : "object",
      "items" : {
        "type" : "object",
        "$ref" : "#"
      }
    },

    "id" : {
      "type" : "string",
      "forbids" : ["$ref"]
    },

    "$ref" : {
      "type" : "string",
      "forbids" : ["id", "description", "format", "properties", "declarations", "items", "required", "requires", "forbids"]
    },

    "items" : {
      "type" : "object",
      "$ref" : "#",
      "forbids" : ["properties"]
    },

    "required" : {
      "type" : "boolean"
    },

    "requires" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    },

    "forbids" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    }

  }

}