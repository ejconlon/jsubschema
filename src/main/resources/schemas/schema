{

  "type" : "object",

  "id" : "http://exathunk.net/schemas/schema",

  "properties" : {

    "type" : {
      "type" : "string",
      "required" : true
    },

    "description" : {
      "type" : "string"
    },

    "format" : {
      "type" : "string"
    },

    "properties" : {
      "type" : "object",
      "items" : {
        "type" : "object",
        "getRef" : "#"
      }
    },

    "id" : {
      "type" : "string",
      "forbids" : ["ref"]
    },

    "ref" : {
      "type" : "string",
      "forbids" : ["id"]
    },

    "items" : {
      "type" : "object",
      "ref" : "#"
    },

    "required" : {
      "type" : "boolean"
    },

    "requires" : {
      "type" : "array",
      "items" : "string"
    },

    "forbids" : {
      "type" : "array",
      "items" : "string"
    }

  }

}