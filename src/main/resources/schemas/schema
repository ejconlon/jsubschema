{

  "type" : "object",

  "id" : "http://exathunk.net/schemas/schema",

  "properties" : {

    "type" : {
      "type" : "string",
      "required" : true,
      "forbids" : ["$ref"]
    },

    "description" : {
      "type" : "string"
    },

    "format" : {
      "type" : "string"
    },

    "properties" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      },
      "forbids" : ["items"]
    },

    "declarations" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      }
    },

    "id" : {
      "type" : "string",
      "forbids" : ["$ref"]
    },

    "$ref" : {
      "type" : "string",
      "forbids" : ["id", "description", "format", "properties", "declarations", "items", "required", "requires", "forbids", "type"]
    },

    "items" : {
      "$ref" : "#"
    },

    "required" : {
      "type" : "boolean"
    },

    "requires" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    },

    "forbids" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    }

  }

}