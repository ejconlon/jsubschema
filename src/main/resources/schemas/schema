{

  "type" : "object",

  "id" : "http://exathunk.net/schemas/schema",

  "properties" : {

    "type" : {
      "type" : "string",
      "required" : true,
      "forbids" : ["$ref"]
    },

    "description" : {
      "type" : "string"
    },

    "format" : {
      "type" : "string"
    },

    "properties" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      }
    },

    "declarations" : {
      "type" : "object",
      "items" : {
        "$ref" : "#"
      }
    },

    "id" : {
      "type" : "string",
      "forbids" : ["$ref"]
    },

    "$ref" : {
      "type" : "string",
      "required" : true,
      "forbids" : ["id", "description", "format", "properties", "declarations", "items", "type"]
    },

    "items" : {
      "$ref" : "#",
      "forbids" : ["properties"]
    },

    "required" : {
      "type" : "boolean"
    },

    "requires" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    },

    "forbids" : {
      "type" : "array",
      "items" : {
          "type": "string"
      }
    }

  }

}